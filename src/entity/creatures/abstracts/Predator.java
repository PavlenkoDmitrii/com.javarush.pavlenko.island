package entity.creatures.abstracts;

public abstract class Predator extends Animal {

    public Predator(double weight, int maxCountOnLocation, int maxSpeed, double maxSatiety) {
        super(weight, maxCountOnLocation, maxSpeed, maxSatiety);
    }
}
